# @frs/web

React + Vite + shadcn/ui admin UI.

## Auth pages

- `/login` — multi-role sign-in
- `/field`, `/approvals`, `/office`, `/system` — role-based home after login

## Run

```bash
npm run dev:web
```

Proxies `/api` → `http://localhost:4000`.
