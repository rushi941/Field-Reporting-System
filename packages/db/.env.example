# Neon or local Postgres — only DATABASE_URL is required.

# Neon Dashboard → Connection string (add ?sslmode=require if missing):
# Prefer the **Direct** (non-pooled) URL for Prisma migrate on Render.
DATABASE_URL="postgresql://USER:PASSWORD@ep-XXXX.REGION.aws.neon.tech/neondb?sslmode=require"
